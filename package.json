{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Todo List Application with CI/CD Pipeline",
  "private": true,
  "workspaces": [
    "packages/client",
    "packages/server"
  ],
  "scripts": {
    "install:all": "npm install && cd packages/client && npm install && cd ../server && npm install",
    "test": "cd packages/server && npm test",
    "build": "cd packages/server && npm run build && cd ../client && npm run build",
    "dev": "concurrently \"cd packages/server && npm run dev\" \"cd packages/client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
